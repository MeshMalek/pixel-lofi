import 'package:flutter/material.dart';

class AppColors {
  // خلفية التطبيق
  static const background = Color(0xFF2C2F4A);

  // كروت / Containers
  static const surface = Color(0xFF2A2A40);

  // لون دافي (زرار – highlight)
  static const accent = Color(0xFFFFC857);

  // نصوص
  static const textPrimary = Color(0xFFF5F5F5);
  static const textSecondary = Color(0xFFB5B5B5);
}
